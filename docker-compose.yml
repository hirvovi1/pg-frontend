services:
  app:
    build:
      context: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
      - playwright-cache:/root/.cache/ms-playwright
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"

volumes:
  playwright-cache: